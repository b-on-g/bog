# Angular Calculator Error Demo

Angular requires `ng new` to scaffold properly.
This folder shows the key component code.

To run:
```bash
ng new angular-calc --standalone --style=css --routing=false
# Replace src/app/app.component.ts with the one in this folder
cd angular-calc && ng serve
```
